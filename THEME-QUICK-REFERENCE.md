# 🎨 主题系统快速参考

## 🌟 核心特性

| 功能 | 说明 |
|------|------|
| **深色模式** | VS Code风格，低蓝光护眼 |
| **浅色模式** | Windows 10风格，高对比度 |
| **一键切换** | 顶部导航栏 🌙/☀️ 按钮 |
| **自动保存** | localStorage 持久化存储 |
| **系统同步** | 首次加载自动检测系统主题 |
| **Canvas适配** | 网格、电场、磁场颜色自动切换 |

## 🎯 快速开始

### 切换主题
```
1. 点击顶部右侧的 🌙 (深色) 或 ☀️ (浅色) 按钮
2. 所有UI和Canvas元素立即切换颜色
3. 主题选择自动保存
```

### 主题检测优先级
```
1. 检查 localStorage 中的用户偏好
   ↓
2. 若无偏好，检测系统主题设置 (macOS/Windows)
   ↓
3. 默认使用深色模式
```

## 🛠️ 开发者API

### ThemeManager 类

```javascript
import { ThemeManager } from './js/utils/ThemeManager.js';

// 创建实例
const themeManager = new ThemeManager();

// 获取当前主题
const currentTheme = themeManager.getCurrentTheme();
// 返回: 'dark' 或 'light'

// 切换主题
themeManager.toggle();

// 保存主题偏好
themeManager.saveTheme('dark');

// 获取主题信息
const info = themeManager.getThemeInfo();
// {
//   current: 'dark',
//   isDark: true,
//   options: ['light', 'dark', 'auto']
// }

// 获取CSS变量值
const colors = themeManager.getThemeColors();
// {
//   bgPrimary: '#1e1e1e',
//   textPrimary: '#cccccc',
//   accentBlue: '#0e639c',
//   ...
// }
```

## 🎨 颜色方案

### 深色模式 (Dark)

```css
/* 背景和文字 */
--bg-primary: #1e1e1e;        /* 主背景 */
--bg-secondary: #252526;      /* 次级背景 */
--text-primary: #cccccc;      /* 主文字 */
--text-secondary: #9d9d9d;    /* 次级文字 */

/* 强调色 */
--accent-blue: #0e639c;       /* VS Code蓝 */
--success-green: #4ec9b0;     /* 青绿 */
--error-red: #f48771;         /* 橙红 */

/* 场颜色 */
--electric-field-color: rgba(255, 200, 0, 0.3);    /* 金黄 */
--magnetic-field-color: rgba(100, 150, 255, 0.3);  /* 蓝紫 */
```

### 浅色模式 (Light)

```css
/* 背景和文字 */
--bg-primary: #ffffff;         /* 主背景 */
--bg-secondary: #f5f5f5;       /* 次级背景 */
--text-primary: #333333;       /* 主文字 */
--text-secondary: #666666;     /* 次级文字 */

/* 强调色 */
--accent-blue: #0078d4;        /* Windows蓝 */
--success-green: #107c10;      /* 绿色 */
--error-red: #f7630c;          /* 橙色 */

/* 场颜色 */
--electric-field-color: rgba(255, 200, 0, 0.15);   /* 金黄淡色 */
--magnetic-field-color: rgba(100, 150, 255, 0.15); /* 蓝紫淡色 */
```

## 📱 Canvas 颜色适配

### GridRenderer - 网格线
```javascript
// 深色主题
'rgba(51, 51, 51, 0.5)'    // 深灰半透

// 浅色主题
'rgba(200, 200, 200, 0.5)' // 浅灰半透
```

### FieldVisualizer - 电场箭头
```javascript
// 深色主题
'rgba(255, 200, 0, 0.6)'   // 金黄

// 浅色主题
'rgba(255, 150, 0, 0.7)'   // 深黄
```

### FieldVisualizer - 磁场符号
```javascript
// 深色主题 - 点
'rgba(100, 150, 255, 0.6)' // 蓝紫

// 浅色主题 - 点
'rgba(100, 150, 255, 0.7)' // 蓝紫

// 深色主题 - 叉
'rgba(200, 100, 200, 0.6)' // 洋红

// 浅色主题 - 叉
'rgba(200, 100, 200, 0.7)' // 洋红
```

## 🧪 测试

### 集成测试
```bash
cd c:\Users\HP\Downloads\新电场
python test_theme_integration.py
```

### 交互式测试
打开 `test-theme.html` 在浏览器中：
- 实时查看主题变量
- 测试Canvas颜色适配
- 验证localStorage持久化

## 🐛 常见问题

### Q: 为什么主题没有完全切换？
**A**: 检查浏览器控制台(F12)是否有错误。确保：
- ThemeManager.js 正确加载
- HTML中有 `id="theme-toggle-btn"` 按钮
- CSS中有 `body.light-theme` 选择器

### Q: 页面刷新后主题重置？
**A**: 这是浏览器隐私设置问题。检查：
- localStorage 是否被禁用
- 浏览器隐私模式是否开启
- 网站是否被添加到例外列表

### Q: Canvas元素颜色仍为固定值？
**A**: 更新GridRenderer.js和FieldVisualizer.js中的颜色检测逻辑。

## 📚 相关文件

| 文件 | 用途 |
|------|------|
| `js/utils/ThemeManager.js` | 主题管理核心模块 |
| `styles/theme.css` | CSS变量定义 |
| `js/main.js` | 主程序集成 |
| `index.html` | 主题按钮UI |
| `js/rendering/GridRenderer.js` | 网格主题适配 |
| `js/rendering/FieldVisualizer.js` | 场可视化主题适配 |
| `THEME-GUIDE.md` | 详细文档 |
| `test-theme.html` | 交互式测试页面 |
| `test_theme_integration.py` | 自动化测试脚本 |

## 🚀 版本信息

- **版本**: v1.2.0
- **发布日期**: 2025-12-06
- **状态**: ✅ 完全功能实现
- **测试**: ✅ 6/6 集成测试通过

---

**💡 提示**: 更多信息请参考 `THEME-GUIDE.md`
